
<div align="center">
  <img src="https://images.unsplash.com/photo-1519125323398-675f0ddb6308?auto=format&fit=crop&w=600&q=80" alt="Projects Priority" width="340" style="border-radius: 16px; margin-bottom: 12px;" />
  <h1>🏅 Project Priority & Polish Tasks</h1>
</div>

---

## 🔥 High priority (finish first)
- <b>rgr</b> (microservices: user, restaurant, order)
  - Add <code>README.md</code> quickstart with <code>docker compose --profile all up -d</code> example and required env vars.
  - Add <code>.env.example</code> and note ports/DB creds.
  - Add a short section: how auth works (Keycloak, tokens), example curl to register/login.
  - Add one integration test for user-service and one for restaurant-service.
  - Add Docker Compose healthcheck and one-line troubleshooting tips.

- <b>the-witcher / LOL</b> (Java games)
  - Add build instructions (Maven/Gradle) and produce runnable JAR.
  - Add screenshots/gif and short gameplay note.
  - Add <code>run.sh</code> / <code>run.bat</code> for demo.

---

## 🟡 Medium priority
- <b>KABAN</b> (Python)
  - Ensure <code>README.md</code> has quickstart, sample data, and DB migration commands.
  - Add <code>docker-compose</code> for local DB if simple.

- <b>php</b> (Laravel theater site)
  - Ensure <code>README.md</code> shows Docker quickstart, <code>.env.example</code>, migrations & seeders run.
  - Add demo screenshots and instructions for running tests.

---

## 🟢 Low/optional
- <b>JAVA_COURSE</b> (React), labs, node-nodejs-basics
  - Polish READMEs, add screenshots, minimal CI (lint/test) if easy.

---

## 📝 Portfolio/CV
- <code>rsschool-cv</code> — update with links to polished projects, one-line tech stack per project, and short description (role/responsibility).

---

> [!tip] <b>Estimate per high-priority repo:</b> 2–5 days of focused work each (docs + small fixes + docker/test). Prioritize <b>rgr</b> first because it matches backend roles best.






